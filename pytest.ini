[tool:pytest]
DJANGO_SETTINGS_MODULE = transmitata.settings
testpaths = .
python_files = test_*.py *_test.py
addopts = -v --tb=short
markers =
    unit: Unit tests
    integration: Integration tests
    slow: Slow running tests